// Top-level build file for the LifeLog phone companion app.
plugins {
    // Plugin versions are resolved by the build environment (Android Studio).
    // Keeping this file minimal makes it easier to open/import.
}

